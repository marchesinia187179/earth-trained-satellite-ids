% ==============================================================================
% CONFIGURAZIONE CENTRALE STILI LISTATI
% ==============================================================================

\lstdefinestyle{python_thesis}{
    language=Python,
    morekeywords={
        as,
        assert,
        async,
        await,
        break,
        class,
        continue,
        def,
        del,
        elif,
        else,
        except,
        finally,
        for,
        from,
        global,
        if,
        import,
        in,
        is,
        lambda,
        nonlocal,
        pass,
        raise,
        return,
        try,
        while,
        with,
        yield
    },
    emph={[2]None,True,False,NotImplemented,Ellipsis},
    emphstyle={[2]\color{purple}\bfseries},
    emph={[3]
        bool,
        dict,
        enumerate,
        float,
        int,
        len,
        list,
        max,
        min,
        print,
        range,
        round,
        set,
        str,
        sum,
        tuple,
        type,
        zip
    },
    emphstyle={[3]\color{indigo}},
    basicstyle=\ttfamily\footnotesize,
    backgroundcolor=\color{backcolour},
    frame=single,
    rulecolor=\color{mygray},
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{mygreen},
    stringstyle=\color{mymauve},
    showstringspaces=false,
    showspaces=false,
    showtabs=false,
    keepspaces=true,
    columns=fullflexible,
    tabsize=4,
    breaklines=true,
    breakatwhitespace=false,
    numbers=left,
    numberstyle=\scriptsize\color{mygray},
    numbersep=8pt,
    captionpos=t
}